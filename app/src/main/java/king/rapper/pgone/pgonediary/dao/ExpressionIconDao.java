package king.rapper.pgone.pgonediary.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import king.rapper.pgone.pgonediary.entity.ExpressionIcon;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "EXPRESSION_ICON".
*/
public class ExpressionIconDao extends AbstractDao<ExpressionIcon, Long> {

    public static final String TABLENAME = "EXPRESSION_ICON";

    /**
     * Properties of entity ExpressionIcon.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property ExpressionIconId = new Property(0, Long.class, "expressionIconId", true, "_id");
        public final static Property Url = new Property(1, String.class, "url", false, "URL");
        public final static Property Type = new Property(2, String.class, "type", false, "TYPE");
        public final static Property Time = new Property(3, Long.class, "time", false, "TIME");
    }


    public ExpressionIconDao(DaoConfig config) {
        super(config);
    }
    
    public ExpressionIconDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"EXPRESSION_ICON\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: expressionIconId
                "\"URL\" TEXT," + // 1: url
                "\"TYPE\" TEXT," + // 2: type
                "\"TIME\" INTEGER);"); // 3: time
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"EXPRESSION_ICON\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ExpressionIcon entity) {
        stmt.clearBindings();
 
        Long expressionIconId = entity.getExpressionIconId();
        if (expressionIconId != null) {
            stmt.bindLong(1, expressionIconId);
        }
 
        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(2, url);
        }
 
        String type = entity.getType();
        if (type != null) {
            stmt.bindString(3, type);
        }
 
        Long time = entity.getTime();
        if (time != null) {
            stmt.bindLong(4, time);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ExpressionIcon entity) {
        stmt.clearBindings();
 
        Long expressionIconId = entity.getExpressionIconId();
        if (expressionIconId != null) {
            stmt.bindLong(1, expressionIconId);
        }
 
        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(2, url);
        }
 
        String type = entity.getType();
        if (type != null) {
            stmt.bindString(3, type);
        }
 
        Long time = entity.getTime();
        if (time != null) {
            stmt.bindLong(4, time);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ExpressionIcon readEntity(Cursor cursor, int offset) {
        ExpressionIcon entity = new ExpressionIcon( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // expressionIconId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // url
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // type
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3) // time
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ExpressionIcon entity, int offset) {
        entity.setExpressionIconId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUrl(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setType(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTime(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ExpressionIcon entity, long rowId) {
        entity.setExpressionIconId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ExpressionIcon entity) {
        if(entity != null) {
            return entity.getExpressionIconId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ExpressionIcon entity) {
        return entity.getExpressionIconId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
